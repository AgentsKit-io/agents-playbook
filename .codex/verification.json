{
  "schemaVersion": 1,
  "project": "agents-playbook-harness-phase-23",
  "root": "..",
  "stateDir": ".codex/verification/harness-phase-23",
  "profile": "strict",
  "contract": {
    "intent": "Make the harness CLI and Playbook CI dogfood path reproducible from a clean checkout.",
    "scope": {
      "inScope": ["package.json", ".github/workflows/ci.yml", "scripts/verify-harness-ci.mjs", "scripts/verify-harness-consumer.mjs", "packages/harness", ".codex/verification.json"],
      "outOfScope": ["provider-specific agent adapters", "automatic release publication", "repository product changes"]
    },
    "ambiguities": [],
    "outcomes": [
      { "id": "package", "statement": "The package tests, built CLI, and packed consumer path pass from the repository workspace.", "checks": ["harness-tests", "harness-cli", "harness-consumer"] },
      { "id": "ci-dogfood", "statement": "Playbook CI explicitly installs, tests, invokes, and consumes the harness on Node 22.", "checks": ["harness-ci"] },
      { "id": "docs", "statement": "The package documents the CI dogfood contract and records the structural decision.", "checks": ["harness-docs"] }
    ]
  },
  "surfaces": {
    "logic": true,
    "endpoint": { "required": false, "reason": "No endpoint behavior is changed." },
    "database": { "required": false, "reason": "No database behavior is changed." },
    "cli": true,
    "mcp": { "required": false, "reason": "No MCP behavior is changed." },
    "ui": { "required": false, "reason": "No UI is changed." },
    "docs": true
  },
  "checks": [
    { "id": "harness-tests", "category": "logic", "command": "pnpm harness:test", "evidence": "structured", "timeoutMs": 120000 },
    { "id": "harness-cli", "category": "cli", "execution": "real", "command": "pnpm harness:cli", "evidence": "structured", "timeoutMs": 120000 },
    { "id": "harness-consumer", "category": "cli", "execution": "real", "command": "node scripts/verify-harness-consumer.mjs", "evidence": "structured", "timeoutMs": 180000 },
    { "id": "harness-ci", "category": "logic", "command": "node scripts/verify-harness-ci.mjs", "evidence": "structured", "timeoutMs": 30000 },
    { "id": "harness-docs", "category": "docs", "command": "node scripts/verify-harness-package.mjs && pnpm check:doc-bridge-config && pnpm check:readme-standard", "evidence": "structured", "timeoutMs": 120000 }
  ],
  "tracking": { "required": false, "reason": "Local-first release-path validation." },
  "budget": { "maxDurationMs": 600000 },
  "cleanup": { "roots": [".codex/verification/tmp"] }
}
