{
  "schemaVersion": 1,
  "project": "agents-playbook-harness-phase-44",
  "root": "..",
  "stateDir": ".codex/verification/harness-phase-44",
  "profile": "strict",
  "contract": {
    "intent": "Expose absolute acceptance and protocol deltas when relative improvement has a zero baseline.",
    "scope": {
      "inScope": ["packages/harness/src/metrics.ts", "packages/harness/test/metrics.test.ts", "scripts/run-agentskit-os-baseline.mjs", "scripts/verify-agentskit-os-baseline.mjs", "packages/harness/README.md", "packages/harness/docs/ADR-0039-artifact-and-protocol-metrics.md", "packages/harness/CHANGELOG.md", ".codex/verification.json"],
      "outOfScope": ["agentskit-os product code", "provider implementation changes", "benchmark reruns", "UI and real-browser validation", "database and MCP adapters", "external tracking", "enterprise release claim"]
    },
    "ambiguities": [],
      "outcomes": [
      { "id": "zero-baseline-delta", "statement": "Comparisons expose absolute artifact and protocol deltas when relative improvement has a zero baseline.", "checks": ["metrics"] },
      { "id": "fail-closed", "statement": "Protocol completion metrics do not make incomplete baseline or harness data comparable.", "checks": ["metrics"] },
      { "id": "docs", "statement": "The metric contract and its limitations are documented.", "checks": ["harness-docs"] }
    ]
  },
  "surfaces": {
    "logic": true,
    "endpoint": { "required": false, "reason": "No endpoint behavior is changed." },
    "database": { "required": false, "reason": "No database behavior is changed." },
    "cli": { "required": false, "reason": "No CLI behavior is changed." },
    "mcp": { "required": false, "reason": "No MCP behavior is changed." },
    "ui": { "required": false, "reason": "No UI is changed." },
    "docs": true
  },
  "checks": [
    { "id": "metrics", "category": "logic", "execution": "real", "command": "pnpm --filter @agentskit/harness test:metrics && node -e \"console.log(JSON.stringify({status:'passed',criteria:['zero-baseline-delta','fail-closed']}))\"", "evidence": "structured", "timeoutMs": 120000 },
    { "id": "harness-docs", "category": "docs", "command": "node scripts/verify-harness-package.mjs && pnpm check:doc-bridge-config && pnpm check:readme-standard && node -e \"console.log(JSON.stringify({status:'passed',criteria:['docs']}))\"", "evidence": "structured" }
  ],
  "tracking": { "required": false, "reason": "Local deterministic protocol validation." },
  "budget": { "maxDurationMs": 300000 },
  "cleanup": { "roots": [".codex/verification/tmp"] }
}
