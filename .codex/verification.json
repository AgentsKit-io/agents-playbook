{
  "schemaVersion": 1,
  "project": "agents-playbook-harness-phase-25",
  "root": "..",
  "stateDir": ".codex/verification/harness-phase-25",
  "profile": "strict",
  "contract": {
    "intent": "Prove that signed harness evidence is portable outside the source workspace.",
    "scope": {
      "inScope": ["scripts/verify-harness-cli-flow.mjs", "packages/harness", ".codex/verification.json"],
      "outOfScope": ["new evidence formats", "remote artifact storage", "provider-specific adapters"]
    },
    "ambiguities": [],
    "outcomes": [
      { "id": "package", "statement": "The harness package test suite and public CLI flow remain valid.", "checks": ["harness-tests", "harness-cli-flow"] },
      { "id": "evidence-portability", "statement": "A signed evidence bundle verifies after being copied to an isolated directory with its trusted public key.", "checks": ["harness-cli-flow"] },
      { "id": "docs", "statement": "The package documents the portability guarantee and records the structural decision.", "checks": ["harness-docs"] }
    ]
  },
  "surfaces": {
    "logic": true,
    "endpoint": { "required": false, "reason": "No endpoint behavior is changed." },
    "database": { "required": false, "reason": "No database behavior is changed." },
    "cli": true,
    "mcp": { "required": false, "reason": "No MCP behavior is changed." },
    "ui": { "required": false, "reason": "No UI is changed." },
    "docs": true
  },
  "checks": [
    { "id": "harness-tests", "category": "logic", "command": "pnpm --filter @agentskit/harness test:signed-bundle", "evidence": "structured", "timeoutMs": 120000 },
    { "id": "harness-cli-flow", "category": "cli", "execution": "real", "command": "node scripts/verify-harness-cli-flow.mjs", "evidence": "structured", "timeoutMs": 120000 },
    { "id": "harness-docs", "category": "docs", "command": "node scripts/verify-harness-package.mjs && pnpm check:doc-bridge-config && pnpm check:readme-standard", "evidence": "structured", "timeoutMs": 120000 }
  ],
  "tracking": { "required": false, "reason": "Local deterministic portability validation." },
  "budget": { "maxDurationMs": 300000 },
  "cleanup": { "roots": [".codex/verification/tmp"] }
}
