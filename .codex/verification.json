{
  "schemaVersion": 1,
  "project": "agents-playbook-harness-phase-27",
  "root": "..",
  "stateDir": ".codex/verification/harness-phase-27",
  "profile": "strict",
  "contract": {
    "intent": "Separate automated CI preparation from real human approval.",
    "scope": {
      "inScope": ["packages/harness", "scripts/run-harness-ci-evidence.mjs", "scripts/verify-harness-ci.mjs", ".github/workflows/ci.yml", ".codex/verification.json"],
      "outOfScope": ["real agentskit-os benchmark corpus", "browser, database, and MCP adapters", "central observability", "KMS and external identity integration"]
    },
    "ambiguities": [],
    "outcomes": [
      { "id": "ci-approval", "statement": "The API and CLI distinguish CI preparation from human contract approval, and only human approval can complete a verified run.", "checks": ["harness-ci-approval"] },
      { "id": "ci-evidence", "statement": "The CI evidence flow uses automated preparation and ends at AWAITING_HUMAN_APPROVAL without fabricating a human actor.", "checks": ["harness-ci-evidence", "harness-ci-contract"] },
      { "id": "docs", "statement": "The package documents the honest CI preparation protocol and records the architectural decision.", "checks": ["harness-docs"] }
    ]
  },
  "surfaces": {
    "logic": true,
    "endpoint": { "required": false, "reason": "No endpoint behavior is changed." },
    "database": { "required": false, "reason": "No database behavior is changed." },
    "cli": true,
    "mcp": { "required": false, "reason": "No MCP behavior is changed." },
    "ui": { "required": false, "reason": "No UI is changed." },
    "docs": true
  },
  "checks": [
    { "id": "harness-ci-approval", "category": "logic", "command": "pnpm --filter @agentskit/harness test:ci-approval", "evidence": "structured", "timeoutMs": 120000 },
    { "id": "harness-ci-evidence", "category": "cli", "execution": "real", "command": "node scripts/run-harness-ci-evidence.mjs --fixture", "evidence": "structured", "timeoutMs": 120000 },
    { "id": "harness-ci-contract", "category": "cli", "execution": "real", "command": "node scripts/verify-harness-ci.mjs", "evidence": "structured", "timeoutMs": 120000 },
    { "id": "harness-docs", "category": "docs", "command": "node scripts/verify-harness-package.mjs && pnpm check:doc-bridge-config && pnpm check:readme-standard", "evidence": "structured", "timeoutMs": 120000 }
  ],
  "tracking": { "required": false, "reason": "Local deterministic protocol validation." },
  "budget": { "maxDurationMs": 300000 },
  "cleanup": { "roots": [".codex/verification/tmp"] }
}
