{
  "schemaVersion": 1,
  "project": "agents-playbook-harness-phase-28",
  "root": "..",
  "stateDir": ".codex/verification/harness-phase-28",
  "profile": "strict",
  "contract": {
    "intent": "Define and validate a provenance-bearing bridge from the AgentsKit OS coding benchmark to honest harness evidence.",
    "scope": {
      "inScope": ["packages/harness", "benchmarks/agentskit-os-phase-28.json", "scripts/verify-harness-agentskit-os-benchmark.mjs", ".codex/verification.json"],
      "outOfScope": ["paid provider execution", "changing agentskit-os product code", "browser, database, and MCP adapters", "central observability", "KMS and external identity integration"]
    },
    "ambiguities": [],
    "outcomes": [
      { "id": "benchmark-corpus", "statement": "The manifest identifies a reproducible AgentsKit OS task corpus with immutable source and prompt provenance.", "checks": ["harness-benchmark-corpus"] },
      { "id": "benchmark-integrity", "statement": "The harness validates manifest metadata and rejects malformed or non-comparable benchmark inputs.", "checks": ["harness-benchmark-integrity"] },
      { "id": "agentskit-os-bridge", "statement": "A real AgentsKit OS benchmark configuration and report can be validated without converting heuristic provider output into human acceptance.", "checks": ["harness-agentskit-os-bridge"] },
      { "id": "docs", "statement": "The package documents the portable benchmark bridge and its current measurement limits.", "checks": ["harness-docs"] }
    ]
  },
  "surfaces": {
    "logic": true,
    "endpoint": { "required": false, "reason": "No endpoint behavior is changed." },
    "database": { "required": false, "reason": "No database behavior is changed." },
    "cli": true,
    "mcp": { "required": false, "reason": "No MCP behavior is changed." },
    "ui": { "required": false, "reason": "No UI is changed." },
    "docs": true
  },
  "checks": [
    { "id": "harness-benchmark-corpus", "category": "logic", "command": "node scripts/verify-harness-agentskit-os-benchmark.mjs --manifest benchmarks/agentskit-os-phase-28.json --target \"$AGENTSKIT_OS_ROOT\"", "evidence": "structured", "timeoutMs": 120000 },
    { "id": "harness-benchmark-integrity", "category": "logic", "command": "pnpm --filter @agentskit/harness test:benchmark-bridge", "evidence": "structured", "timeoutMs": 120000 },
    { "id": "harness-agentskit-os-bridge", "category": "cli", "execution": "real", "command": "node scripts/verify-harness-agentskit-os-benchmark.mjs --manifest benchmarks/agentskit-os-phase-28.json --target \"$AGENTSKIT_OS_ROOT\" --report \"$AGENTSKIT_OS_BENCHMARK_REPORT\"", "evidence": "structured", "timeoutMs": 120000 },
    { "id": "harness-docs", "category": "docs", "command": "node scripts/verify-harness-package.mjs && pnpm check:doc-bridge-config && pnpm check:readme-standard", "evidence": "structured", "timeoutMs": 120000 }
  ],
  "tracking": { "required": false, "reason": "Local deterministic protocol validation." },
  "budget": { "maxDurationMs": 300000 },
  "cleanup": { "roots": [".codex/verification/tmp"] }
}
